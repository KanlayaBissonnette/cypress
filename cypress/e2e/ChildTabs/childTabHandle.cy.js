describe('Child tab handle', () => {
    it('the-internet.herokuapp window handle', () => {
        cy.visit('https://the-internet.herokuapp.com/windows')
        cy.get('a[href=' / windows / new ']').invoke('removeAttr', 'taget').click()
    })
})